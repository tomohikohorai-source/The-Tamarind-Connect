
import React from 'react';
import { ExternalLink } from 'lucide-react';

interface Props {
  index?: number;
}

export const AffiliateBanner: React.FC<Props> = ({ index }) => {
  return null; // Temporarily hidden for AdSense review
};
